<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
  </head>
  <body>
    <ul id="topic-list">
      <% for (var i = 0; i < topics?.length; i++) {%> <% if(!topics[i].isNsfw ||
      isAuthenticated ){ %>
      <li>
        <a href="<%= `${topicsUri}/${topics[i]._id}`  %> "
          ><%= topics[i].name %>
        </a>
      </li>
      <% } %> <% } %>
    </ul>
  </body>
</html>
